import { colors } from './colors';
import { sizes } from './sizes';
import { spacings } from './spacings';
import { typography } from './typography';

export const theme = { spacings, sizes, colors, typography };
